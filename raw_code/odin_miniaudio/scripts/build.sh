## Figure out how to check if it isn't built yet?
# make -C "/Users/kuba/Documents/source_code/Odin/vendor/miniaudio/src"
#odin build src/reload.odin -file -build-mode:shared -out:dist/reload.so -define:RAYLIB_SHARED=true -vet -debug