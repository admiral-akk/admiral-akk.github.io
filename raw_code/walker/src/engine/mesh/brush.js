class Brush {
  constructor(planes) {
    this.planes = planes;
  }

  triangles() {
    // generate verts at the interesection of 3 planes
    const verts = [];

    // wrap verts in a convex hull via gift wrapping

    // return triangles

    return verts;
  }
}
